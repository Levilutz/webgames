image:
  pullPolicy: Always
  repository:
  tag:

postgres:
  pvc:
    size: 10Gi
    storageClass: vultr-block-storage

replicas: 1

ingress:
  enabled: false
  host:
  prefix:
  tls:
    enabled: false
    secretName:

sendgrid:
  required: false
  secretName:
